# Copy this file to .env and fill in values as needed.

# Optional: a default PostgreSQL connection string for local dev/testing.
# The frontend can also supply a connection string at runtime via POST /connect.
DEFAULT_CONNECTION_STRING=postgresql://username:password@localhost:5432/database

# Query execution timeout in seconds (used in Phase 2 /run endpoint)
QUERY_TIMEOUT_SECONDS=30
